import { MatCardTitlePipe } from './mat-card-title.pipe';

describe('MatCardTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new MatCardTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
